[workspace]
members = [".", "nft", "multi_asset"]
resolver = "2"

[package]
name = "rwa-marketplace"
version = "0.2.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "27.0.6"

[profile.release]
opt-level = "z"
lto = true
debug = false

[dev-dependencies]
soroban-sdk = { version = "27.0.6", features = ["testutils"] }
proptest = "1.4"
share-certificate-nft = { path = "nft" }
stellar-tokens = { version = "0.7.1" }
